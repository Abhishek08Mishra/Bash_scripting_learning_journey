#!/bin/bash

echo "Enter your name"

read name

echo "Welcome, $name !"

# The read command is used to take input from the user.
# This script asks the user to enter their name, stores the input in a variable called "name",
# and then displays a greeting message in the terminal: "Welcome, <name>!"
